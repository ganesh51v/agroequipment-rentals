// This file is for custom Cypress commands.
// For now, it can be empty or include basic commands if needed.

// Example:
// Cypress.Commands.add('login', (email, password) => {
//   cy.get('#email').type(email);
//   cy.get('#password').type(password);
//   cy.get('form').submit();
// });
